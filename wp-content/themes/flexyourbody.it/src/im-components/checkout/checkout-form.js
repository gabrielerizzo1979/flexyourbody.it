const checkoutForm = () => {
    // jQuery(document).ready(function($) {
    //     // Inizializza selectWoo sul tuo elemento
    //     $('#billing_country').selectWoo({ passive: true });
    //     let selectedOptionValue;
    //     let selectedOptionText;
    //     $('#billing_country').val('IT').trigger('change');
    //     $('#billing_country').on('select2:select', function(e) {
    //         selectedOptionValue = $(this).val(); // Ottieni il valore dell'option selezionato
    //         selectedOptionText = $(this).find('option:selected').text(); // Ottieni il testo dell'option selezionato
    //         $('#billing_cf').val('');
    //         if ((selectedOptionValue === 'IT') && (selectedOptionText === 'Italia')) {
    //             $('#billing_cf_field').show();
    //         } else {
    //             $('#billing_cf_field').hide();
    //         }
    //     });
    // });
};

export default checkoutForm;
